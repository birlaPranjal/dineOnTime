{
  "version": 2,
  "buildCommand": "npm install && npm run vercel-build",
  "installCommand": "npm install",
  "outputDirectory": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  }
}

